# Environment Configuration for Personal Asset Dashboard

# CORS Settings
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# Database (if you want to make it configurable in the future)
# DATABASE_URL=sqlite:///./personal_asset_dash.db

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# API Keys (for future use if needed)
# Add your API keys here instead of hardcoding them
